use pyo3::prelude::*;
use std::fs;

fn main() -> PyResult<()> {
    let url = "http://youtube.com/watch?v=2lAe1cqCOXo";

    Python::with_gil(|py| {
        let pytube = PyModule::;
        Ok(())
    })
}
